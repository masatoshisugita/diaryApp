<div class="row">
  <div class="offset-3 col-md-6">

<h1 class="mt-3">Editing User</h1>

<%= render 'form', user: @user %>
<%= link_to 'ユーザー削除',[@user],method: :delete,data: { confirm: "タスク「#{@user.name}」を削除します。よろしいですか？" } %>
</div>
</div>
